# repositories/

Reserved for a future refactor.

For Version 1, all database access lives in `app/crud/`. This folder is kept
only as documentation for a possible future repository-pattern refactor. Do not
duplicate CRUD responsibilities here. If it causes confusion, delete it.
